# ==============================================================================
# Create source and header for PART
# ==============================================================================
set(COMPONENT_PART_DIR
    ${CMAKE_CURRENT_SOURCE_DIR}/PART
)

set(COMPONENT_PART_SOURCE
    # Add the path to each source file here
    ${COMPONENT_PART_DIR}/src/
)

set (COMPONENT_PART_HEADERS
    ${COMPONENT_PART_DIR}/include
)

