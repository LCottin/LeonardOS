name: OS CI/CD Pipeline

on:
  workflow_dispatch:        # manual trigger from any branch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: echo "test"    # sanity check to verify workflow is running
